# Evernode Profile Hub — Frontend
#
# Set to 'true' to run with simulated contract responses (no HotPocket servers needed).
VITE_MOCK_MODE=true

# Comma-separated wss:// URLs for HotPocket contract nodes.
# Only used when VITE_MOCK_MODE=false.
VITE_CONTRACT_URLS=wss://localhost:8081
